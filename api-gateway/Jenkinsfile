pipeline {
    agent any
 tools {
        maven 'local_maven'
    }

    stages {
        
        stage('api-gateway') {
            steps {
                    sh 'mvn clean package -Dmaven.test.skip=true'
                }
            }
                
        }

        
        }
